# Vercel Dynamic DNS Configuration
# Copy this file to .env and fill in your values

# Required: Your Vercel API token
# Get this from: https://vercel.com/account/tokens
VERCEL_TOKEN=your_vercel_api_token_here

# Required: The domain you want to update (without subdomain)
VERCEL_DOMAIN=italicninja.com

# Optional: The record name to update (default: @ for root domain)
# Examples: @ (root), www, api, etc.
VERCEL_RECORD_NAME=@

# Optional: Record type (default: A)
# Usually A for IPv4 addresses
VERCEL_RECORD_TYPE=A

# Optional: Check interval in seconds (default: 300 = 5 minutes)
# Minimum recommended: 60 seconds
CHECK_INTERVAL=300

# Optional: Timezone (default: UTC)
# Examples: America/Vancouver, Europe/London, etc.
TZ=UTC