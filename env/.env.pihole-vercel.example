# Pi-hole + Vercel DNS Configuration
# Copy this file to .env and fill in your values

# ===================================
# Pi-hole Configuration
# ===================================

# Required: Pi-hole web interface password
PIHOLE_PASSWORD=your_strong_password_here

# Optional: Pi-hole image tag (default: latest)
IMAGE_TAG=latest

# Local network configuration
LOCAL_IPV4=192.168.1.10
TZ=America/Vancouver

# Domain for Pi-hole (local or public)
# If using Vercel, this should match your Vercel domain
PIHOLE_DOMAIN=pihole.italicninja.com

# Your public IP address (for DNS records)
SERVER_PUBLIC_IP=

# ===================================
# Vercel DNS Configuration
# ===================================

# Required: Your Vercel API token
# Get this from: https://vercel.com/account/tokens
VERCEL_TOKEN=your_vercel_api_token_here

# Required: The domain you want to manage (without subdomain)
# This should be a domain you've added to Vercel
VERCEL_DOMAIN=italicninja.com

# Optional: The primary record name to update (default: @ for root domain)
# Examples: @ (root), www, home, etc.
VERCEL_RECORD_NAME=@

# Optional: Record type (default: A for IPv4)
VERCEL_RECORD_TYPE=A

# Optional: Subdomain for Pi-hole access
# This creates a DNS record like pihole.yourdomain.com pointing to your public IP
# Leave empty to skip creating a Pi-hole subdomain
PIHOLE_SUBDOMAIN=pihole

# Optional: Check interval in seconds (default: 300 = 5 minutes)
# How often to check for IP changes
CHECK_INTERVAL=300

# ===================================
# Usage Examples
# ===================================

# Example 1: Root domain + Pi-hole subdomain
# VERCEL_DOMAIN=italicninja.com
# VERCEL_RECORD_NAME=@
# PIHOLE_SUBDOMAIN=pihole
# Result: italicninja.com -> your IP, pihole.italicninja.com -> your IP

# Example 2: Home subdomain + Pi-hole
# VERCEL_DOMAIN=italicninja.com
# VERCEL_RECORD_NAME=home
# PIHOLE_SUBDOMAIN=pihole
# Result: home.italicninja.com -> your IP, pihole.italicninja.com -> your IP

# Example 3: Just root domain (no Pi-hole subdomain)
# VERCEL_DOMAIN=italicninja.com
# VERCEL_RECORD_NAME=@
# PIHOLE_SUBDOMAIN=
# Result: italicninja.com -> your IP only
